# Backend API environment variables
# Ask the orchestrator/user to set these in backend_api/.env

# PostgreSQL connection string (recommended)
# Example: postgresql://appuser:dbuser123@localhost:5000/myapp
DATABASE_URL=

# CORS origins (comma-separated). Example: http://localhost:3000
CORS_ALLOW_ORIGINS=
